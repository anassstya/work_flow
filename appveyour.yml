image: Ubuntu1804 

stack: node 21

branche: master

cache: node_modules 

install:
  - yarn 

build: off 

build_script:
  - yarn build 

test_script:
  - yarn lint && yarn test

deploy_script: 
- git push origin master